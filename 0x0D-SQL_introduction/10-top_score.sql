-- This script lists all records, in descending order by score, the score and name
-- of all records in the table of the passed in database argument

SELECT score, name FROM second_table
ORDER BY score DESC;
